import React, { useState } from 'react';
import {
    View,
    StyleSheet,
    Dimensions,
} from 'react-native';

const AddProductScreen = () => {
    // Bilgiler gösterilecek 
    // Altta da ekleme kısmı olacak
}

const styles = StyleSheet.create({

});

export default AddProductScreen;